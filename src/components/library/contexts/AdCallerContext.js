// /* eslint-disable react/prop-types */
// import React, { createContext, useContext, useRef } from 'react';

// const AdCallerContext = createContext(null);

// export function useAdCallerContext() {
//   return useContext(AdCallerContext);
// }

// export default function AdCallerContextProvider({ children }) {
//   const AdsPageLoadArray = useRef([]);
//   return (
//     <AdCallerContext.Provider value={AdsPageLoadArray.current}>
//       {children}
//     </AdCallerContext.Provider>
//   );
// }
